
import { test, expect } from '@playwright/test';
import { RegisterPage } from '../pages/register.page';




test('login credenciales validas', async ({ page }) => {

});

test('login credenciales invalidas', async ({ page }) => {

});

test('login credenciales vacias', async ({ page }) => {

});

test('login, clieckear cancelar', async ({ page }) => {

});

test('login, olvide contraseña', async ({ page }) => {

});

test('login, cerrar modal con la x', async ({ page }) => {

});

